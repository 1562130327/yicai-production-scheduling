# 给 Claude 的指令文件

你是溢彩包装生产排单系统的开发助手。读完这个文件就能理解整个项目，知道要做什么。

---

## 一、项目背景

溢彩包装是一个 **7人小工厂**，做 EVA 材料加工。师傅分工明确，工艺流程固定。

**核心需求**：
1. 跟单员录入订单 → 管理员分配师傅 → 师傅按工序干活 → 完成
2. 师傅手机上能看到任务，完成后自动通知下一个人
3. 管理员能看到所有订单进度
4. 库存不够时自动预警
5. 数据不能丢

**技术栈**：
- 前端：原生 HTML/CSS/JS，无框架
- 后端：Node.js + Express
- 启动：`npm start`（即 `node server-mock.js`）
- 访问：http://localhost:3000

---

## 二、关键文件

```
production-scheduling/
├── server-mock.js              # 主服务器（所有 API，1185行）
├── models/data-store.js        # 数据模型 + 状态机（470行）
├── schedule-data.js            # 48条订单静态数据
├── public/                     # 前端页面（23个 HTML）
│   ├── index.html              # 主页
│   ├── schedule.html           # 排单总表（订单列表）
│   ├── new-order.html          # 录入新订单
│   ├── worker-dashboard.html   # 师傅工作台
│   ├── admin-schedule.html     # 智能调度
│   ├── workshop-map.html       # 车间地图
│   └── ...
├── PROJECT-HANDOFF.md          # 设计文档（部分过时）
├── MES-LOGIC.md                # MES 逻辑链详解（必读）
└── ISSUES.md                   # 问题清单
```

---

## 三、订单状态机（已实现，不要改坏）

```
订单状态：待分配 → 已分配 → 生产中 → 已完成
                            ↘ 已取消 → 待分配（可恢复）

工序状态：待开始 → 进行中 → 已完成
                ↕
              暂停

自动推导规则（根据工序状态推导订单状态）：
- 无分配记录 → 待分配
- 全部待开始 → 已分配
- 有进行中/暂停 → 生产中
- 全部已完成 → 已完成
```

---

## 四、师傅岗位

| 师傅 | 岗位 | 机器 | 零工 | 说明 |
|------|------|------|------|------|
| 郑思远 | 横竖分切机 | 横竖分切机 | 1-3人 | 打边、分切 |
| 伍乾进 | 破片机 | 破片机 | 3人 | 开片 |
| 莫齐国 | 直切机 | 直切机 | 0人 | 精准切片 |
| 李乐 | 冲床（全部） | 4台冲床 | 2-5人 | 冲型 |
| 周忠琼 | 冲床辅助 | 冲床 | 可调岗 | 辅助李乐 |
| 简翠花 | 背胶/点胶 | 热熔胶机/点胶机 | 1人 | 背胶、点胶 |
| 杨合进 | 打包 | - | 0人 | 最后一步 |

管理员：潘光龙、潘朝森
跟单员：罗巧芳、曾小芳、彭鸿媛

---

## 五、工艺流程

### 基础工艺
- 片材：横竖分切→破片
- 片材切片：横竖分切→破片→直切
- 片材冲型：横竖分切→破片→冲床
- 片材切片冲型：横竖分切→破片→直切→冲床
- 切片冲型：直切→冲床

### 背胶工艺
- 片材背胶：横竖分切→破片→背胶
- 片材背胶冲型：横竖分切→破片→冲床→背胶
- 片材背胶切片：横竖分切→破片→背胶→直切
- 片材背胶切片冲型：横竖分切→破片→背胶→直切→冲床

### 贴布工艺
- 片材贴布：横竖分切→破片→贴布（外发）
- 片材贴布冲型：横竖分切→破片→贴布→冲床
- 片材贴布切片：横竖分切→破片→贴布→直切
- 片材贴布切片冲型：横竖分切→破片→贴布→直切→冲床

### 点胶工艺（冲型后点胶，与背胶互斥）
- 片材切片冲型点胶：横竖分切→破片→直切→冲床→点胶
- 片材背胶冲型点胶：横竖分切→破片→冲床→背胶→点胶
- 片材背胶切片冲型点胶：横竖分切→破片→背胶→直切→冲床→点胶
- 片材贴布切片冲型点胶：横竖分切→破片→贴布→直切→冲床→点胶

### 库存工艺
- 库存片材：直接出库
- 库存切片：直切
- 库存冲型：冲床
- 库存切片冲型：直切→冲床

---

## 六、问题清单（按优先级）

### P0 - 必须先做

#### 1. 数据重启丢失
- 现状：所有数据存在内存变量里，服务器重启全丢
- 目标：数据持久化到 JSON 文件，启动时加载，变更时保存
- 方案：在 data-store.js 里加 `saveToFile()` / `loadFromFile()`，用 `fs` 读写 `data/*.json`

#### 2. 新建订单不能保存
- 现状：`new-order.html` 表单提交只弹 alert，没调 API
- 目标：提交后调 `POST /api/schedule` 创建订单，状态为"待分配"
- 注意：后端要新增 `POST /api/schedule` 接口

#### 3. 没有权限控制
- 现状：登录只存 localStorage，任何人可直接访问所有页面和 API
- 目标：师傅只看自己的任务，跟单员只看自己录的单，管理员看全部
- 方案：简单的 token 机制，API 加权限校验中间件

#### 4. 反馈系统不联动
- 现状：反馈能提交，但不影响订单状态，不通知任何人
- 目标：反馈提交后订单暂停，通知相关人员，处理后恢复

### P1 - 尽快做

#### 5. 工序流程缺"打包"步骤
- 现状：WORKFLOW_MAP 里大部分工艺没有打包作为最后一步
- 目标：所有工艺流程最后都加一步"打包→杨合进"

#### 6. 车间地图数据不真实
- 现状：机器状态硬编码
- 目标：根据 assignments 数据动态显示

#### 7. 日报/绩效用随机数
- 现状：`/api/daily-report` 里师傅完成数是 `Math.random()`
- 目标：从真实数据统计

#### 8. 导出 Excel 没实现
- 现状：按钮只弹 alert
- 目标：用 SheetJS 生成 Excel 下载

#### 9. 登录页名字不一致
- 现状：登录页显示"老莫"、"老莫老婆"，data-store 里是"莫齐国"、"简翠花"
- 目标：统一用真名

### P2 - 有空再做

10. 库存 Excel 路径硬编码（改为可配置）
11. PROJECT-HANDOFF.md 过时（更新状态机描述）
12. 订单编辑功能不完整（加编辑按钮）
13. 移动端适配（响应式布局）

---

## 七、核心逻辑链（MES 模式）

详细实现见 `MES-LOGIC.md`，这里列出要点：

### 逻辑链1：工序自动传导（事件驱动）
师傅完成工序 → 触发事件 → 自动通知下一个师傅 → 更新订单状态

```javascript
// 用 Node.js EventEmitter 实现
const EventEmitter = require('events')
const bus = new EventEmitter()

bus.on('工序完成', (data) => {
  // 1. 自动推导订单状态
  syncOrderStatus(data.orderId)
  // 2. 通知下一个师傅
  notifyWorker(data.nextWorker, '轮到你了')
})

// 在完成工序API里触发
bus.emit('工序完成', { orderId, stepName, worker, nextWorker })
```

### 逻辑链2：资源调度
分配订单时自动检查：师傅有没有空？机器有没有空？

### 逻辑链3：物料管理
订单创建时算用料 → 领料扣库存 → 库存低预警 → 追溯

### 逻辑链4：反馈处理
师傅提反馈 → 暂停订单 → 通知相关人 → 处理后恢复

### 逻辑链5：数据采集
师傅每次操作记录时间、数量 → 统计效率 → 发现瓶颈

### 逻辑链6：权限控制
```javascript
// 中间件示例
function requireAuth(req, res, next) {
  const token = req.headers.authorization?.replace('Bearer ', '')
  const user = verifyToken(token)
  if (!user) return res.status(401).json({ error: '未登录' })
  req.user = user
  next()
}

function requireRole(...roles) {
  return (req, res, next) => {
    if (!roles.includes(req.user.role)) {
      return res.status(403).json({ error: '没有权限' })
    }
    next()
  }
}
```

### 逻辑链7：实时通知
- 方式1：WebSocket（页面内弹窗）
- 方式2：飞书机器人推送（推荐）
- 方式3：微信小程序订阅消息

---

## 八、实现顺序

### 第一周（P0）
1. 数据持久化（JSON文件）
2. 新建订单API
3. 权限控制（基础版）
4. 事件系统（EventEmitter）
5. 反馈联动

### 第二周（P1）
6. 工序自动传导（事件驱动通知）
7. 库存联动（领料+预警）
8. 飞书/微信通知
9. 导出Excel

### 第三周（P2）
10. 工时记录 + 日报统计
11. 车间地图真实数据
12. 订单编辑功能
13. 移动端适配

---

## 九、给 Claude 的实现原则

1. **每个功能都要有事件**：做完一个动作要触发事件，不能默默完成
2. **状态机不能跳级**：待分配→已分配→生产中→已完成，只能按顺序
3. **自动推导**：订单状态从工序状态推导，不用手动设置
4. **通知要到位**：每个事件都要通知相关人员
5. **数据要持久**：所有数据存JSON文件，重启不丢
6. **权限要校验**：后端校验，不是前端隐藏
7. **保持简单**：不要引入复杂框架，用 Node.js 原生功能
8. **不要改坏现有功能**：状态机、分配逻辑、工序流程已经能用

---

## 十、参考文档

- `MES-LOGIC.md` - MES 逻辑链详解，包含完整代码示例
- `ISSUES.md` - 问题清单，按优先级排序
- `PROJECT-HANDOFF.md` - 原始设计文档（部分过时，以本文件为准）
